# vi:syntax=zsh

function chpwd {
    if [[ -e .info ]]; then
        echo ">>>"
        cat .info
        #pygmentize -g .info
        echo ">>>"
    fi

    if type odrive  > /dev/null; then
        echo "Odrive:" && odrive syncstate . | head -1
    fi}
